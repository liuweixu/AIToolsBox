import { ChatMessageError } from "../types";
export declare const getMessageError: (response: Response) => Promise<ChatMessageError>;
type SSEFinishType = 'done' | 'error' | 'abort' | 'progress';
export interface FetchSSEOptions {
    onErrorHandle?: (error: ChatMessageError) => void;
    onMessageHandle?: (text: string, response: Response, type: SSEFinishType) => void;
    onAbort?: (text: string) => Promise<void>;
    onFinish?: (text: string, type: SSEFinishType) => Promise<void>;
    signal?: AbortSignal;
}
/**
 * 使用流式方法获取数据
 * @param fetchFn
 * @param options
 */
export declare const processSSE: (response: Response, options?: FetchSSEOptions) => Promise<Response>;
export {};
